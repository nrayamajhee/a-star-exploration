image: nrayamajhee/arch-wasm-pack

pages:
  stage: deploy
  script:
  - ./build b 
  #- wasm-pack test --headless --chrome
  - cd www
  - npm run build
  - mv dist ../public
  artifacts:
    paths:
    - public
  only:
  - master